// In Project 1 there is exactly one building. This constant is the value fed to
// useBuildingLiveState(); in Project 2 it is replaced by a route param.
export const CURRENT_BUILDING_ID = 'default';

/** Pixels of shaft height per floor. Drives car positioning + animation. */
export const FLOOR_PX = 56;
